<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alexolmedo.lab6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IngresoCasos"
            android:label="Ingreso de Casos"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ConsultaCasos"
            android:label="Consulta de Casos"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ResultadoBusqueda"
            android:label="Resultado de búsqueda"
            android:parentActivityName=".ConsultaCasos" />
        <activity
            android:name=".activity_lista_casos"
            android:label="Lista de Casos"></activity>
    </application>

</manifest>